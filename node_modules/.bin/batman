#!/bin/sh
if [ -x "`dirname "$0"`/node" ]; then
  "`dirname "$0"`/node"  "`dirname "$0"`/../batman/tools/batman" "$@"
  ret=$?
else 
  node  "`dirname "$0"`/../batman/tools/batman" "$@"
  ret=$?
fi
exit $ret
